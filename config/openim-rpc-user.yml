rpc:
  # API or other RPCs can access this RPC through this IP; if left blank, the internal network IP is obtained by default
  registerIP: 
  # Listening IP; 0.0.0.0 means both internal and external IPs are listened to, if blank, the internal network IP is automatically obtained by default
  listenIP: 0.0.0.0
  # autoSetPorts indicates whether to automatically set the ports
  autoSetPorts: true
  # List of ports that the RPC service listens on; configuring multiple ports will launch multiple instances. These must match the number of configured prometheus ports
  # It will only take effect when autoSetPorts is set to false.
  ports: [ 10320 ]

prometheus:
  # Whether to enable prometheus
  enable: true
  # Prometheus listening ports, must be consistent with the number of rpc.ports
  # It will only take effect when autoSetPorts is set to false.
  ports: [ 12320 ]

# GetUserByPhone: false = ignore phone_visibility when searching by phone; true = enforce phone_visibility (hidden / friends-only).
phoneSearchVisibility: false

# Default call ringtone for newly registered users (used when the field is not provided in the register request).
callRingtoneDefaults:
  url: https://sok-wallet.s3.ap-southeast-1.amazonaws.com/20250430/207387952bc74b979b454b4be116e06a.mp3
  name: 索爱经典铃音
  cover: https://sok-wallet.s3.ap-southeast-1.amazonaws.com/20250430/3549b49c7c764c20a8d610d07e1d3e17.png

# 用户首次上线时，由官方服务号推送的欢迎通知（每个用户仅发一次）。
welcomeNotification:
  # 是否启用
  enable: true
  # 发送方服务号账号，必须是通知账号（AppMangerLevel>=3）
  sendID: service_notification_bot
  # 服务通知分类：1安全 2账号 3系统 4版本更新
  subType: 3
  # 语言未匹配/未设置时使用的兜底桶键
  defaultLanguage: en
  # 各语言文案。用户 language（如 zh-CN/zh-TW/en-US）会归一化到以下桶键：
  #   zh-CN/zh-Hans/zh/zh-SG -> zh-hans；zh-TW/zh-HK/zh-MO/zh-Hant -> zh-hant；其余 -> defaultLanguage
  # 注意：Viper 会把 map key 小写化，因此这里直接使用小写桶键。
  languages:
    zh-hans:
      title: SOK
      content: 欢迎来到 SOK！端到端加密，无人可读。我是官方服务号，负责推送通知与安全提醒。请放心，这里只有你和TA。
    zh-hant:
      title: SOK
      content: 歡迎來到 SOK！端到端加密，無人可讀。我是官方服務號，負責推送通知與安全提醒。請放心，這裡只有你和 TA。
    en:
      title: SOK
      content: "Welcome to SOK! End-to-end encrypted—no one else can read your chats. I'm the official service account for notifications and security alerts. Rest assured, it's just you and them here."
