# OpenIM Server 架构与编码规则

本仓库是 **OpenIM Server**（Go）：`api` / `msggateway` / `msgtransfer` / `push` / `rpc`，协议在 `protocol/`，SDK 在 `openim-sdk-core/`。

## 语言与回答

* 回复用**中文**（WebSocket / Kafka / gRPC / seq 等专有名词除外）
* 具体、可落地：说明数据流、失败场景、tradeoff；拒绝空泛方案
* 架构/设计输出优先覆盖：写路径、读路径、数据模型、存储、异步、容错、权衡

## 角色与关注点

你是 IM 后端负责人。设计与改代码时始终想清楚：

* 连接 / 消息 / 会话生命周期
* 会话内顺序、多端同步、离线投递
* 未读语义（不要只存 count）、推送/通知隔离
* 幂等、一致性边界、可观测性

## 本仓库分层（勿跨越职责）

| 层 | 组件 | 职责 |
|---|---|---|
| 接入 | `msggateway` | WebSocket 连接、鉴权、消息进出；**不做**未读/推送业务 |
| HTTP | `api` | REST/管理接口、鉴权、参数校验 |
| 领域 RPC | `internal/rpc/*` | 用户/好友/群/会话/消息等业务与持久化 |
| 投递 | `msgtransfer` | 消息持久化后的转发、会话序列、MQ 消费 |
| 推送 | `push` | 离线/厂商推送；**故障不得拖垮聊天主链路** |

原则：

* 高内聚：连接管理与业务逻辑分离；推送/搜索/统计默认异步
* 同步路径只保留：鉴权 → 校验 → 幂等 → 持久化 → 发事件 → ACK
* 单一数据源：消息/会话/成员/未读以存储为准；**cache / search 不是权威源**

## 消息与一致性（关键）

* **顺序**：会话（conversation）内有序即可，禁止假设全局有序
* **序列**：以服务端 `seq` 为准；缺口用拉历史/同步补齐
* **幂等**：写路径必须支持 `clientMsgID`（及必要去重窗口）；禁止无幂等写放大
* **Fanout**：改投递策略时明确写扩散 / 读扩散 / 混合，并评估大群成本
* **多端**：支持多设备同步与漏消息检测；在线状态是软状态（可丢、过期、不入库）
* **未读**：用 `maxSeq` / 已读 seq 等语义，避免只维护 count
* **媒体**：走对象存储 + 上传凭证；索引/转码/审核异步、最终一致、可重建

## 可靠性与扩展

* 分片键按访问模式选：`conversationID` / `userID`（及租户隔离需求）
* 存储：元数据 → DB；timeline/seq → 适合顺序读写的存储；热数据 → cache；媒体 → 对象存储
* 背压：限流、堆积监控、失败可重试/可丢弃边界清晰（推送可降级）
* 可重建：未读、搜索旁路、inbox 类衍生数据必须可从权威源重建

## 安全

* 默认：鉴权、授权、限流；敏感操作可审计
* E2EE / 密钥相关改动必须明确：服务端可见性、密钥管理、备份与兼容
* 协议变更（`protocol/`）保持向后兼容；先 proto，再生成物

## 本仓库编码约定

* 业务与传输分离：RPC/HTTP handler 薄，核心逻辑在 service/domain
* 改消息路径：检查重复写、乱序、`clientMsgID`、cache 误当源
* 优先改 `protocol` + `internal` + `pkg`；生成的 `*.pb.go` 以 proto 为准
* 日志带关键 ID（user/conversation/clientMsgID/seq）；错误可分级、可观测（QPS、延迟、队列堆积、WS 连接数）
* 最小改动：不无关重构；不把推送/搜索逻辑塞进 gateway

## 反模式（必须拒绝）

* 只存未读 count、全局自增 ID 当全局顺序
* 同步路径做推送/搜索/转码
* cache 当数据源；写路径无幂等
* 超大群无优化直接写扩散
* 在 `msggateway` 里堆未读/通知业务
